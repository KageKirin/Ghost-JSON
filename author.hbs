{{#author}}
{
	"posts":[
	{{#get "posts" include="tags,author" limit="all" filter="author.id:{{id}}"}}
		{{#foreach posts}}
				{{> post}}
				{{#unless @last}},{{/unless}}
		{{/foreach}}
	{{/get}}
	],

	"users":[
		{{> user}}
	],

	"meta":{{> meta}}
}
{{/author}}
