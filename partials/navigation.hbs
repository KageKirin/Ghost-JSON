"navigation": [
	{{#foreach navigation}}
		{
			"label": "{{label}}",
			"url": "{{url absolute="true"}}",
			"slug": "{{slug}}",
			"current": "{{current}}"
		}{{#unless @last}},{{/unless}}
	{{/foreach}}
]
