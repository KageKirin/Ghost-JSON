{{#tag}}
{
	"posts":[
	{{#get "posts" include="tags,author" limit="all" filter="tags.id:{{id}}"}}
		{{#foreach posts}}
				{{> post}}
				{{#unless @last}},{{/unless}}
		{{/foreach}}
	{{/get}}
	],

	"tags":[
		{{> tag}}
	],

	"meta":{{> meta}}
}
{{/tag}}
